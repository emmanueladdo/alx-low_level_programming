#include <stdio.h>
/**
 * main - Entry point
 *
 * Description - Prints hello world
 * Return: Always 0 (successful)
 */
int main(void)
{
char mywords = "Programming is like building a multilingual puzzle";
puts(mywords);
return (0);
}
